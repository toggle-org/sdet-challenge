version: "3.8"

services:
  dev:
    image: node:18-bullseye
    working_dir: /workspace
    volumes:
      - ..:/workspace:cached
    ports:
      - "3000:3000"
      - "3001:3001"
    command: sleep infinity
    environment:
      - NODE_ENV=development
